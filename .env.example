# 環境変数設定例
# 実際の値に置き換えて使用してください

# Slack Bot Token (xoxb- で始まるトークン)
# Slack アプリの "OAuth & Permissions" から取得
export SLACK_BOT_TOKEN="xoxb-your-bot-token-here"

# Slack チャンネルID (C で始まるID)
# チャンネル名を右クリック → "リンクをコピー" から取得可能
export SLACK_CHANNEL_ID="C1234567890"

# 監視する株式銘柄 (JSON形式、オプション)
# 設定しない場合はデフォルト（トヨタ、リクルート）が使用されます
export STOCK_SYMBOLS='[
  {
    "symbol": "215A.T",
    "name": "タイミー",
    "market": "東証グロース"
  },
  {
    "symbol": "7974.T",
    "name": "任天堂",
    "market": "東証プライム"
  }
]'

# 監視するインデックス (JSON形式、オプション)
# 設定しない場合はデフォルト（日経平均、TOPIX）が使用されます
export INDEX_SYMBOLS='[
  {
    "symbol": "^N225",
    "name": "日経平均株価"
  },
  {
    "symbol": "^TPX",
    "name": "TOPIX"
  }
]'

# 使用方法:
# 1. 上記の値を実際のトークンとチャンネルIDに置き換える
# 2. ターミナルで以下を実行:
#    source .env.example
# または直接実行:
#    export SLACK_BOT_TOKEN="your-actual-token"
#    export SLACK_CHANNEL_ID="your-actual-channel-id"
